apply from: '../../module.gradle'

android {

    defaultConfig {
        versionCode 100
        versionName "1.0.0"
    }

}

dependencies {
    implementation fileTree(dir: "libs", include: ["*.jar"])
    api project(':libs:ext')
}